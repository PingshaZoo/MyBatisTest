<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="City">
    <select  id="findCityByID"  parameterType="int"   resultType="firstMyBatis.batis.po.City">
     select * from city where id = #{id}
    </select>
    <select  id="findCityByName" parameterType="java.lang.String"  resultType="firstMyBatis.batis.po.City">
         select * from city where name like '%${value}%'
    </select>
    <insert id="insertCity" parameterType="firstMyBatis.batis.po.City">
    insert into city(id,name,countryCode,district,population) values(#{id},#{name},#{countryCode},#{district},#{population})
    </insert>
</mapper>